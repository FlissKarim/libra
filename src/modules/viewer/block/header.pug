style(type="text/css").
    .container {
        display: flex;
    }
    .item {
        flex-grow: 1;
        height: 100px;
    }
h1=trans_field
div.container
  img.item(src=logo)
  span
    strong #{label}
    strong #{adresse}
    strong #{codeZip}